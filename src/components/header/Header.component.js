import React from "react";

import { HeaderView, HeaderText, Icon } from "./Header.component.styles";

export const Header = () => {
	return (
		<HeaderView>
			<Icon />
			<HeaderText>DARTS</HeaderText>
		</HeaderView>
	);
};
