import { colors } from "./colors";
import { space, borderRadius } from "./spacing";
import { sizes } from "./sizing";
import { fonts, fontWeights, fontSizes } from "./fonts";

export const theme = {
	colors,
	space,
	sizes,
	borderRadius,
	fonts,
	fontWeights,
	fontSizes,
};
